# Core lab dependencies
# Install torch separately with the CUDA wheel matching your driver, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# This file lists the rest.

triton
torchprofile
jupyterlab
matplotlib
pandas
